# Environment Variables

# Web URL for sharing links (REQUIRED for production)
# This is the domain where your landing pages are hosted
# Example: EXPO_PUBLIC_WEB_URL=https://yourdomain.com
EXPO_PUBLIC_WEB_URL=https://sourceimpact.app

# Note: Create a .env.local file with your actual domain
# The .env.local file is gitignored and won't be committed
